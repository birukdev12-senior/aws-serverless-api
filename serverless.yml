service: aws-serverless-api

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  stage: dev

functions:
  api:
    handler: index.handler
    events:
      - http: ANY /{proxy+}
      - http: ANY /
